name: plant_watering_reminder
description: "Plant Watering Reminder - Never forget to water your plants again!"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  intl: ^0.19.0
  path_provider: ^2.1.4
  uuid: ^4.5.1
  google_fonts: ^6.2.1
  flutter_slidable: ^3.1.1
  image_picker: ^1.1.2
  flutter_local_notifications: ^18.0.1
  firebase_core: ^4.6.0
  cloud_firestore: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
